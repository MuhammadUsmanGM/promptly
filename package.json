{
  "name": "promptly",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/rules",
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build -w packages/rules && npm run build -w packages/cli",
    "dev": "npm run dev -w packages/cli"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
